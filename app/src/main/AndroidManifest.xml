<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pl.hexmind.mindshaper">

    <!-- Modern permissions for file access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permission for audio recording - required for voice notes -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permission for managing files -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
            <data android:mimeType="audio/*" />
        </intent>
    </queries>

    <application
        android:name="pl.hexmind.mindshaper.ApplicationMain"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindShaper">

        <activity
            android:name="pl.hexmind.mindshaper.activities.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MindShaper.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="pl.hexmind.mindshaper.activities.details.ThoughtDetailsActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.main.MainActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.capture.ThoughtsCaptureActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.main.MainActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.carousel.ThoughtCarouselActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.main.MainActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.settings.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.MindShaper.NoActionBar"
            android:parentActivityName="pl.hexmind.mindshaper.activities.main.MainActivity"/>

    </application>

</manifest>