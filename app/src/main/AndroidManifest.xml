<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="pl.hexmind.mindshaper">

    <!-- Modern permissions for file access -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- Permission for audio recording - required for voice notes -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Permission for managing files -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <queries>
        <intent>
            <action android:name="android.intent.action.OPEN_DOCUMENT" />
            <data android:mimeType="audio/*" />
        </intent>
    </queries>

    <application
        android:name="pl.hexmind.mindshaper.ApplicationMain"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MindShaper">

        <activity
            android:name="pl.hexmind.mindshaper.activities.home.HomeActivity"
            android:exported="true"
            android:theme="@style/Theme.MindShaper.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="pl.hexmind.mindshaper.activities.details.DetailsActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.home.HomeActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.capture.CaptureActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.home.HomeActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.carousel.CarouselActivity"
            android:exported="false"
            android:parentActivityName="pl.hexmind.mindshaper.activities.home.HomeActivity"
            android:theme="@style/Theme.MindShaper.NoActionBar" />

        <activity
            android:name="pl.hexmind.mindshaper.activities.settings.SettingsActivity"
            android:exported="true"
            android:theme="@style/Theme.MindShaper.NoActionBar"
            android:parentActivityName="pl.hexmind.mindshaper.activities.home.HomeActivity"/>

        <service
            android:name=".common.HexKeyboard"
            android:label="Hex Keyboard"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

    </application>

</manifest>