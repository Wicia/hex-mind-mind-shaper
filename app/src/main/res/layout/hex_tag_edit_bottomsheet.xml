<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/shape_hex_content"
    android:backgroundTint="@color/app_background">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingBottom="80dp"
        android:clipToPadding="false">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Drag handle -->
            <View
                android:layout_width="40dp"
                android:layout_height="4dp"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="12dp"
                android:layout_marginBottom="4dp"
                android:background="@color/_gray_lvl_2"
                />

            <!-- === HEADER === -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_horizontal"
                android:layout_marginBottom="16dp">

                <!-- * Decorator * -->
                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="42dp"
                    android:scaleX="-1"
                    android:scaleY="-1"
                    android:layout_gravity="center_vertical"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/common_placeholder_accessibility_content_desc"
                    app:srcCompat="@drawable/ic_decorator_olive"
                    app:tint="@color/separator_leaf"
                    />

                <!-- Title -->
                <TextView
                    android:id="@+id/tv_dialog_title"
                    style="@style/DialogTile"
                    android:textColor="@color/text_secondary"
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:text="@string/metadata_tag_edit_title"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:layout_marginBottom="0dp"
                    />

                <!-- * Decorator * -->
                <ImageView
                    android:layout_width="wrap_content"
                    android:layout_height="42dp"
                    android:layout_gravity="center_vertical"
                    android:scaleY="-1"
                    android:contentDescription="@string/common_placeholder_accessibility_content_desc"
                    app:srcCompat="@drawable/ic_decorator_olive"
                    app:tint="@color/separator_leaf"
                    />

            </LinearLayout>

            <!-- Tag name -->
            <pl.hexmind.mindshaper.common.ui.views.HexInputField
                android:id="@+id/hif_tag_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="4dp"
                app:hintPlaceholder="@string/metadata_tag_edit_hint_placeholder"
                app:hintFloating="@string/metadata_tag_edit_hint_floating"
                />

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

    <!-- Save -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fab_confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:src="@drawable/ic_action_save"
        app:maxImageSize="32dp"
        app:backgroundTint="@color/button_primary_enabled_background"
        app:fabSize="normal"
        app:elevation="6dp"
        android:contentDescription="@string/common_placeholder_accessibility_content_desc"
        />

</androidx.coordinatorlayout.widget.CoordinatorLayout>